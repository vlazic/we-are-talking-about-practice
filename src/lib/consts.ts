const MILISECONDS = 1 / 1000;
const SECOND = 1;
const MINUTE = SECOND * 60;
const HOUR = MINUTE * 60;

const consts = Object.freeze({
  MILISECONDS,
  SECOND,
  MINUTE,
  HOUR
});

export default consts;
